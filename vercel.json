{
  "framework": null,
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install --legacy-peer-deps",
  "rewrites": [
    { "source": "/((?!assets|images|fonts).*)", "destination": "/index.html" }
  ]
}
